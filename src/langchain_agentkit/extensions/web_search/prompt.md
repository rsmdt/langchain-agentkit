## Web Search

You have access to the `WebSearch` tool which searches multiple providers simultaneously.

When you call `WebSearch(query="your search query")`, it will:
- Query all configured search providers in parallel
- Return results grouped by provider

Use WebSearch when you need to find current information, verify facts,
or research topics. Results will include source attribution.

Configured providers: {provider_names}
